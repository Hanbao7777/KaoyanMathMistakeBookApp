# 考点汇总替换知识地图 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the knowledge map data source with the 考点汇总 (exam point summary), update all related prompts and category definitions, and migrate existing data.

**Architecture:** The app ships a pre-generated `knowledge_map_seed.zip` in resources/. On first launch (when knowledge_points table is empty), it auto-imports the seed data, migrates old category values, and re-matches existing questions to new exam points. The AI prompts and UI category lists are updated to align with the new structure.

**Tech Stack:** TypeScript, Electron, sql.js, AdmZip, React

---

## File Map

| File | Action | Purpose |
|------|--------|---------|
| `src/shared/options.ts` | Modify | Replace CATEGORIES list with new chapter names |
| `src/main/services/deepseekService.ts` | Modify | Update AI prompt with new categories + knowledge_points list |
| `src/main/services/databaseService.ts` | Modify | Add `migrateCategoryValues()` function |
| `src/main/services/knowledgeMapService.ts` | Modify | Add `seedImportKnowledgeMap()` function |
| `src/main/main.ts` | Modify | Wire startup flow: seed → migrate → rematch |
| `resources/knowledge_map_seed.zip` | Create | Pre-generated exam point data (offline step) |
| `prompts/考点数据生成专家.md` | Create | GPT prompt for generating the seed zip |
| `prompts/考研高数错题整理专家_V2.md` | Modify | Update category + knowledge_points lists |
| `prompts/README.md` | Modify | Add reference to new prompt |

---

### Task 1: Update CATEGORIES in options.ts

**Files:**
- Modify: `src/shared/options.ts:1-13`

- [ ] **Step 1: Replace CATEGORIES array**

Open `src/shared/options.ts` and replace the `CATEGORIES` array (lines 1-13) with:

```typescript
export const CATEGORIES = [
  // 高等数学 — 考点汇总 5 章
  '函数、极限、连续',
  '一元函数微分学',
  '一元函数积分学',
  '多元函数微积分学',
  '常微分方程',
  // 保留（考点汇总未覆盖，已有错题需要）
  '无穷级数',
  // 线性代数 — 考点汇总 3 章
  '行列式与矩阵',
  '线性方程组与向量',
  '特征值与二次型',
  // 概率论 — 暂时保留
  '概率论',
  '其他'
];
```

Do NOT change QUESTION_TYPES, ERROR_REASONS, DIFFICULTIES, MASTERY_LEVELS, SOURCES, MATH_SUBJECTS, or REVIEW_RESULTS.

- [ ] **Step 2: Verify no other files hardcode old category values**

Run:
```powershell
Select-String -Path "src\renderer\*.tsx","src\renderer\components\*.tsx" -Pattern "函数、极限与连续|重积分|曲线曲面积分|微分方程" | Select-Object -Property Path,LineNumber,Line
```

Expected: No matches (these files import from options.ts). If any matches are found, they need to be updated to use the imported constant.

- [ ] **Step 3: Commit**

```powershell
git add src/shared/options.ts
git commit -m "feat: update CATEGORIES to match new exam point summary structure"
```

---

### Task 2: Update deepseekService.ts AI prompt

**Files:**
- Modify: `src/main/services/deepseekService.ts:66-86`

- [ ] **Step 1: Replace STRUCTURE_SYSTEM_PROMPT**

Open `src/main/services/deepseekService.ts` and replace the `STRUCTURE_SYSTEM_PROMPT` constant (lines 66-86) with:

```typescript
const STRUCTURE_SYSTEM_PROMPT = `你是考研数学辅导专家，从 OCR 文本中还原并整理错题。

OCR 文本可能有识别错误（导数上标丢失、符号混淆、括号错位等），请根据数学知识主动修正还原。
所有数学公式必须用 $$ 包裹，确保 LaTeX 可直接渲染。

字段要求：
- title: "考点 + 动作 + 对象"，如"高阶导数判定极值"、"二重积分换序求值"，≤15字
- content: 完整还原题目原文，保留所有条件和问题，公式修正后放入 $$
- wrong_thinking: 推测学生可能的错误思考（≥1句具体描述），不要写"计算错误"这种泛泛的词
- correct_solution: 完整解析过程。不限步骤数，大题可包含推导链、分类讨论、多种解法。每步写明原理和计算，公式用 $$。即使 OCR 遗漏了步骤也要根据数学知识补全
- answer: 最终答案的完整数学表达式或数值。如果是证明题写结论，如果是大题写最终结果。不要说"详见上"、"正确"这类占位词
- subject: 高等数学/线性代数/概率论/其他
- category: 必须从以下章节中选择一个：函数、极限、连续/一元函数微分学/一元函数积分学/多元函数微积分学/常微分方程/无穷级数/行列式与矩阵/线性方程组与向量/特征值与二次型/概率论/其他
- question_type: 选择题/填空题/解答题/证明题
- error_reason: 计算错误/概念不清/方法选择错误/审题不清/公式记错/逻辑漏洞/其他
- difficulty: 简单/中等/困难/压轴
- tags: 3-5个关键词
- knowledge_points: 2-4个关联考点，优先从以下考点中选择：
  高等数学：数列敛散性的判定、函数极限的计算、无穷小量、确定极限中的参数、函数的连续性和间断点的类型、曲率与曲率半径、导数和微分的概念、导数和微分的计算、导数的应用、函数的单调性极值和最值、曲线的凹凸性拐点及渐近线、方程根的存在性和个数、不等式的证明、微分中值定理、泰勒公式、不定积分的计算、定积分的概念和性质、定积分的计算、变限积分、反常积分的计算和敛散性、定积分的应用、一元函数积分学综合题、偏导数的概念和计算、全微分的概念和计算、多元函数的极值问题、二重积分的概念和性质、二次积分和变换积分次序、二重积分的计算、二重积分的应用、可分离变量的微分方程、一阶线性微分方程、常系数齐次线性微分方程、微分方程的解和线性微分方程的解的结构、其他方程、二阶常系数非齐次线性微分方程、微分方程的应用
  线性代数：矩阵的运算和变换、伴随矩阵和可逆矩阵、矩阵的秩、向量组的线性相关性、向量组之间的线性表示、特征值和特征向量、矩阵的相似和相似对角化
- raw_ocr_text: 原始 OCR 文本（原样保留）

只返回 JSON，不要其他文字。`;
```

- [ ] **Step 2: Commit**

```powershell
git add src/main/services/deepseekService.ts
git commit -m "feat: update AI prompt with new categories and exam point list"
```

---

### Task 3: Add migrateCategoryValues() to databaseService.ts

**Files:**
- Modify: `src/main/services/databaseService.ts` (add new export function after `initializeDatabase` at line 404)

- [ ] **Step 1: Add the migration function**

Insert the following function after `initializeDatabase()` (after line 404) in `src/main/services/databaseService.ts`:

```typescript
/**
 * Migrate old category values to match the new exam point summary structure.
 * Called once on startup after seed import, before knowledge point re-matching.
 */
export async function migrateCategoryValues(): Promise<{ migrated: number }> {
  const database = await getDb();

  const CATEGORY_MAP: Record<string, string> = {
    '函数、极限与连续': '函数、极限、连续',
    '多元函数微分学': '多元函数微积分学',
    '重积分': '多元函数微积分学',
    '曲线曲面积分': '多元函数微积分学',
    '微分方程': '常微分方程',
    '线性代数': '其他'
  };

  let migrated = 0;
  database.run('BEGIN TRANSACTION');
  try {
    for (const [oldValue, newValue] of Object.entries(CATEGORY_MAP)) {
      database.run(
        "UPDATE questions SET category = ?, updated_at = ? WHERE category = ? AND (deleted_at IS NULL OR deleted_at = '')",
        [newValue, nowIso(), oldValue]
      );
      migrated += 1;
    }
    database.run('COMMIT');
    persist();
  } catch (error) {
    database.run('ROLLBACK');
    throw error;
  }

  return { migrated };
}
```

Note: This function uses `getDb()` (private), `run()` (private), `persist()` (private), and `nowIso()` (private at line 43) — all already exist in databaseService.ts.

- [ ] **Step 2: Verify the function compiles**

Run:
```powershell
npx tsc --noEmit --pretty
```

Expected: No errors related to `migrateCategoryValues`.

- [ ] **Step 3: Commit**

```powershell
git add src/main/services/databaseService.ts
git commit -m "feat: add migrateCategoryValues() for old category data migration"
```

---

### Task 4: Add seedImportKnowledgeMap() to knowledgeMapService.ts

**Files:**
- Modify: `src/main/services/knowledgeMapService.ts` (add new export function after `importKnowledgeMapZip` at line 396)

- [ ] **Step 1: Add the seed import function**

Insert the following function after `importKnowledgeMapZip()` (after line 396) in `src/main/services/knowledgeMapService.ts`:

```typescript
/**
 * Import the bundled knowledge_map_seed.zip from app resources.
 * Used on first launch when knowledge_points table is empty.
 * Reuses the same upsert logic as importKnowledgeMapZip().
 */
export async function seedImportKnowledgeMap(): Promise<KnowledgeMapImportResult> {
  const { app } = require('electron') as typeof import('electron');
  const resourcesDir = app.isPackaged
    ? path.join(process.resourcesPath)
    : path.join(__dirname, '../../resources');
  const zipPath = path.join(resourcesDir, 'knowledge_map_seed.zip');

  if (!fs.existsSync(zipPath)) {
    throw new Error(`内置考点数据包不存在：${zipPath}`);
  }

  const tempDir = path.join(getPaths().temp, `seed-import-${Date.now()}`);
  fs.mkdirSync(tempDir, { recursive: true });

  try {
    const zip = new AdmZip(zipPath);
    ensureSafeZip(zip, tempDir);
    zip.extractAllTo(tempDir, true);

    const textbookJson = path.join(tempDir, 'textbooks.json');
    const pointsJson = path.join(tempDir, 'knowledge_points.json');
    if (!fs.existsSync(textbookJson)) throw new Error('种子数据包缺少 textbooks.json');
    if (!fs.existsSync(pointsJson)) throw new Error('种子数据包缺少 knowledge_points.json');

    const textbookRaw = readJsonFile<Record<string, unknown>>(textbookJson, 'textbooks.json');
    const pointRows = readJsonFile<unknown>(pointsJson, 'knowledge_points.json');
    if (!Array.isArray(pointRows)) throw new Error('knowledge_points.json 必须是数组');

    const textbookSubject = normalizeSubject(textbookRaw.subject);
    const database = await getDatabase();
    const batchId = await createImportBatch({
      type: 'knowledge_map',
      name: asText(textbookRaw.title) || '考点汇总种子数据',
      sourceFileName: 'knowledge_map_seed.zip',
      source: asText(textbookRaw.title),
      metadata: { ...textbookRaw, seed: true }
    });
    const result: KnowledgeMapImportResult = {
      textbookTitle: asText(textbookRaw.title) || '考研数学考点汇总',
      subject: textbookSubject,
      importedCount: 0,
      updatedCount: 0,
      failedCount: 0,
      failures: [],
      copiedPdfPath: null
    };

    database.run('BEGIN TRANSACTION');
    try {
      const copied = { fileName: asText(textbookRaw.file_name), filePath: asText(textbookRaw.file_path), copiedPath: null };
      const textbookId = upsertTextbook(database, textbookRaw, copied);
      recordImportBatchItem(database, batchId, 'textbooks', textbookId);

      for (const row of pointRows) {
        try {
          const status = upsertKnowledgePoint(database, textbookId, (row ?? {}) as Record<string, unknown>, asText(textbookRaw.subject), batchId);
          if (status.status === 'imported') {
            result.importedCount += 1;
            recordImportBatchItem(database, batchId, 'knowledge_points', status.nodeId);
          } else result.updatedCount += 1;
        } catch (error) {
          result.failedCount += 1;
          const current = (row ?? {}) as Record<string, unknown>;
          result.failures.push({
            node_id: asText(current.node_id),
            title: asText(current.title),
            reason: error instanceof Error ? error.message : String(error)
          });
        }
      }

      finalizeImportBatch(database, batchId, result.failedCount && !result.importedCount && !result.updatedCount ? 'failed' : 'active');
      database.run('COMMIT');
      persistDatabase();
    } catch (error) {
      database.run('ROLLBACK');
      throw error;
    }

    return result;
  } finally {
    fs.rmSync(tempDir, { recursive: true, force: true });
  }
}
```

This function reuses the existing helpers already in knowledgeMapService.ts: `ensureSafeZip`, `readJsonFile`, `upsertTextbook`, `upsertKnowledgePoint`, `asText`, `normalizeSubject`, `getDatabase`, `persistDatabase`, `createImportBatch`, `finalizeImportBatch`, `recordImportBatchItem`.

- [ ] **Step 2: Verify the function compiles**

Run:
```powershell
npx tsc --noEmit --pretty
```

Expected: No errors related to `seedImportKnowledgeMap`.

- [ ] **Step 3: Commit**

```powershell
git add src/main/services/knowledgeMapService.ts
git commit -m "feat: add seedImportKnowledgeMap() for bundled exam point data"
```

---

### Task 5: Wire startup flow in main.ts

**Files:**
- Modify: `src/main/main.ts`

- [ ] **Step 1: Add imports**

At the top of `src/main/main.ts`, after the existing imports (after line 11), add:

```typescript
import { seedImportKnowledgeMap, rematchKnowledgePoints } from './services/knowledgeMapService';
import { migrateCategoryValues, getDatabase } from './services/databaseService';
```

Note: Check if `getDatabase` is already imported from databaseService. If so, only add the new imports.

- [ ] **Step 2: Add startup logic after initializeDatabase**

In `src/main/main.ts`, find the `app.whenReady().then(async () => {` block (line 137). After `await initializeDatabase();` (line 140) and before the `try { ensureDailyAutoBackup(); }` block (line 141), insert:

```typescript
    // Seed exam points on first launch, then migrate categories and re-match questions
    try {
      const database = await getDatabase();
      const countResult = database.exec('SELECT COUNT(*) as c FROM knowledge_points WHERE deleted_at IS NULL OR deleted_at = ""');
      const count = countResult.length && countResult[0].values.length ? Number(countResult[0].values[0][0]) : 0;

      if (count === 0) {
        console.log('[Seed] knowledge_points table empty, importing bundled exam points...');
        const seedResult = await seedImportKnowledgeMap();
        console.log(`[Seed] Imported ${seedResult.importedCount} exam points, ${seedResult.failedCount} failed`);
      }

      console.log('[Migration] Migrating old category values...');
      const migrateResult = await migrateCategoryValues();
      console.log(`[Migration] Processed ${migrateResult.migrated} category mappings`);

      if (count === 0) {
        console.log('[Rematch] Re-matching questions to new exam points...');
        const rematchResult = await rematchKnowledgePoints();
        console.log(`[Rematch] Scanned ${rematchResult.scannedQuestions} questions, ${rematchResult.insertedCount} new links, ${rematchResult.unmatchedQuestions} unmatched`);
      }
    } catch (error) {
      console.warn('[StartupSeed]', error);
    }
```

- [ ] **Step 3: Verify the file compiles**

Run:
```powershell
npx tsc --noEmit --pretty
```

Expected: No errors related to the new imports or startup logic.

- [ ] **Step 4: Commit**

```powershell
git add src/main/main.ts
git commit -m "feat: wire seed import + category migration + rematch on startup"
```

---

### Task 6: Generate knowledge_map_seed.zip (offline)

This task generates the seed data file that ships with the app. It uses a GPT prompt to create the JSON files from the source `tishici/考点汇总.txt`.

**Files:**
- Create: `prompts/考点数据生成专家.md`
- Create: `resources/knowledge_map_seed.zip` (generated output)

- [ ] **Step 1: Create the GPT prompt file**

Create `prompts/考点数据生成专家.md` with the following content:

```markdown
# 考点数据生成专家

你是考研数学数据结构专家。你的任务是将考点汇总文本转换为 App 可导入的 `knowledge_map_seed.zip`。

## 输入

我会提供一份考点汇总文本，结构如下：

```
高等数学
1：函数、极限、连续
数列敛散性的判定
函数极限的计算
...
线性代数
一：行列式
1：矩阵
矩阵的运算和变换
...
```

## 输出

生成 `knowledge_map_seed.zip`，包含：

```
knowledge_map_seed.zip
├── textbooks.json
└── knowledge_points.json
```

## textbooks.json 格式

```json
{
  "title": "考研数学考点汇总",
  "subject": "通用",
  "edition": "2026",
  "file_name": null,
  "note": "基于考点汇总自动生成"
}
```

## knowledge_points.json 格式

每个考点是一个对象，字段如下：

```json
{
  "node_id": "gs_ch1_sequence_convergence",
  "parent_node_id": "gs_ch1_limit_continuity",
  "title": "数列敛散性的判定",
  "subject": "高等数学",
  "category": "函数、极限、连续",
  "level": 3,
  "sort_order": 1,
  "book_page": null,
  "pdf_page": null,
  "summary": "判断数列极限是否存在及收敛值的方法，包括单调有界准则、夹逼准则等",
  "core_formulas": ["\\lim_{n\\to\\infty} a_n = L"],
  "common_question_types": ["数列极限", "递推数列极限"],
  "common_error_reasons": ["概念不清", "方法没想到"],
  "tags": ["数列", "敛散性", "单调有界", "夹逼"]
}
```

## 层级结构规则

### 高等数学（3 层）

| 层级 | level | node_id 格式 | parent_node_id |
|------|-------|-------------|----------------|
| 学科（高等数学） | 1 | `gaoshu` | null |
| 章节（如 1：函数、极限、连续） | 2 | `gs_ch1_limit_continuity` | `gaoshu` |
| 考点（如 数列敛散性的判定） | 3 | `gs_ch1_sequence_convergence` | 对应章节 node_id |

### 线性代数（4 层）

| 层级 | level | node_id 格式 | parent_node_id |
|------|-------|-------------|----------------|
| 学科（线性代数） | 1 | `xiandai` | null |
| 大章节（如 一：行列式） | 2 | `xd_determinant` | `xiandai` |
| 子章节（如 1：矩阵） | 3 | `xd_determinant_matrix` | `xd_determinant` |
| 考点（如 矩阵的运算和变换） | 4 | `xd_determinant_matrix_operations` | `xd_determinant_matrix` |

## node_id 命名规则

- 格式：`{subject_prefix}_{chapter_info}_{english_slug}`
- 高等数学前缀：`gs_`
- 线性代数前缀：`xd_`
- 全小写，下划线分隔
- 使用英文单词或常用缩写，不要用拼音
- 稳定不变，作为外键关联目标

## summary 字段规则

- 50-100 字中文描述
- 说明这个考点"考什么"、"怎么考"
- 不要写成教材定义，要写成考试导向的描述

## core_formulas 字段规则

- LaTeX 公式数组
- 只列核心公式，不要列太多
- 使用 `\\` 转义（JSON 中需要双反斜杠）

## common_question_types 字段规则

- 从以下题型中选择 1-3 个最相关的：
  极限计算、数列极限、指数型极限、1的无穷型、等价无穷小、泰勒展开、洛必达法则、夹逼定理、递推数列极限、连续性与间断点、导数与微分、中值定理、单调性与极值、积分计算、定积分应用、多元函数求导、二重积分、级数敛散性、微分方程、矩阵与行列式、特征值与特征向量、概率计算、其他

## common_error_reasons 字段规则

- 从以下错因中选择 1-2 个最相关的：
  概念不清、公式记错、计算失误、方法没想到、审题错误、条件忽略、步骤不严谨、极限转化不规范、等价替换使用不当、展开阶数不足、同类题反复错、其他

## tags 字段规则

- 3-5 个关键词标签
- 用于匹配搜索，简洁准确

## 自检清单

生成前检查：
1. 所有考点都已录入，没有遗漏
2. node_id 全局唯一，无重复
3. parent_node_id 正确指向父节点
4. level 值与层级一致
5. 高等数学考点 category 与章节名一致
6. 线性代数考点 category 使用子章节名（如"矩阵"、"向量"）
7. summary 在 50-100 字之间
8. JSON 格式正确，可解析
9. 数学公式使用 `\\` 转义

## 输出流程

1. 先输出考点统计：总数、各章节数量
2. 输出预览（前 5 个考点的完整 JSON）
3. 询问确认
4. 确认后生成 zip
```

- [ ] **Step 2: Use the prompt to generate the seed zip**

Open ChatGPT (or Claude), paste the prompt from `prompts/考点数据生成专家.md`, then provide the content of `tishici/考点汇总.txt` as input. The AI will generate `knowledge_map_seed.zip`.

- [ ] **Step 3: Place the generated zip in resources/**

Copy the generated `knowledge_map_seed.zip` to `resources/knowledge_map_seed.zip`.

- [ ] **Step 4: Verify the zip contents**

```powershell
# Extract to temp and validate JSON
$tempSeed = "$env:TEMP\seed-verify"
New-Item -ItemType Directory -Force $tempSeed | Out-Null
Expand-Archive -Path "resources\knowledge_map_seed.zip" -DestinationPath $tempSeed -Force
Get-ChildItem $tempSeed
# Should show: textbooks.json, knowledge_points.json

# Validate JSON
Get-Content "$tempSeed\textbooks.json" | ConvertFrom-Json | Select-Object title, subject, edition
# Expected: title=考研数学考点汇总, subject=通用, edition=2026

$points = Get-Content "$tempSeed\knowledge_points.json" | ConvertFrom-Json
Write-Host "Total knowledge points: $($points.Count)"
# Expected: ~42 points (35 高数 + 7 线代)

# Check for duplicate node_ids
$nodeIds = $points | ForEach-Object { $_.node_id }
$duplicates = $nodeIds | Group-Object | Where-Object { $_.Count -gt 1 }
if ($duplicates) { Write-Host "WARNING: Duplicate node_ids found:"; $duplicates } else { Write-Host "No duplicate node_ids" }

# Cleanup
Remove-Item -Recurse -Force $tempSeed
```

Expected: ~42 knowledge points, no duplicate node_ids, valid JSON structure.

- [ ] **Step 5: Commit**

```powershell
git add resources/knowledge_map_seed.zip prompts/考点数据生成专家.md
git commit -m "feat: add bundled exam point seed data and generation prompt"
```

---

### Task 7: Update 错题整理专家 GPT prompt

**Files:**
- Modify: `prompts/考研高数错题整理专家_V2.md`

- [ ] **Step 1: Update category list (Section 六)**

In `prompts/考研高数错题整理专家_V2.md`, find the `category` section (around line 184-198). Replace the category list inside the code block:

Old:
```
函数、极限与连续
一元函数微分学
一元函数积分学
多元函数微分学
重积分
曲线曲面积分
无穷级数
微分方程
线性代数
概率论
其他
```

New:
```
函数、极限、连续
一元函数微分学
一元函数积分学
多元函数微积分学
常微分方程
无穷级数
行列式与矩阵
线性方程组与向量
特征值与二次型
概率论
其他
```

- [ ] **Step 2: Update knowledge_points list (Section 七)**

In the same file, find the `knowledge_points` section (around line 287-360). Replace the entire "常用知识点参考" code block with:

```text
高等数学：
数列敛散性的判定
函数极限的计算
无穷小量
确定极限中的参数
函数的连续性和间断点的类型
曲率与曲率半径
导数和微分的概念
导数和微分的计算
导数的应用
函数的单调性、极值和最值
曲线的凹凸性、拐点及渐近线
方程根的存在性和个数
不等式的证明
微分中值定理
泰勒公式
不定积分的计算
定积分的概念和性质
定积分的计算
变限积分
反常积分的计算和敛散性
定积分的应用
一元函数积分学综合题
偏导数的概念和计算
全微分的概念和计算
多元函数的极值问题
二重积分的概念和性质
二次积分和变换积分次序
二重积分的计算
二重积分的应用
可分离变量的微分方程
一阶线性微分方程
常系数齐次线性微分方程
微分方程的解和线性微分方程的解的结构
其他方程
二阶常系数非齐次线性微分方程
微分方程的应用

线性代数：
矩阵的运算和变换
伴随矩阵和可逆矩阵
矩阵的秩
向量组的线性相关性
向量组之间的线性表示
特征值和特征向量
矩阵的相似和相似对角化
```

- [ ] **Step 3: Commit**

```powershell
git add prompts/考研高数错题整理专家_V2.md
git commit -m "feat: update 错题整理专家 prompt with new categories and exam points"
```

---

### Task 8: Update prompts/README.md

**Files:**
- Modify: `prompts/README.md`

- [ ] **Step 1: Add reference to new prompt**

In `prompts/README.md`, add a new section after the existing content (after line 57):

```markdown
## 3. 考点数据生成专家

用途：

```text
考点汇总.txt
→ 解析章节、考点层级结构
→ 生成 knowledge_map_seed.zip
```

生成文件：

```text
knowledge_map_seed.zip
├── textbooks.json
└── knowledge_points.json
```

此文件打包进 App 的 `resources/` 目录，首次启动时自动导入。
```

Also replace the "推荐使用顺序" section (lines 50-57) with:

```markdown
## 推荐使用顺序

1. App 内置考点汇总数据，首次启动自动导入，无需手动操作。
2. 如需更新考点数据，使用"考点数据生成专家"重新生成 `knowledge_map_seed.zip`。
3. 使用"考研高数错题整理专家 V2"处理错题图片，生成 `wrong_questions_import.zip`。
4. 在 App 中导入错题包。
5. 导入后可在 Dashboard、错题库、知识地图、图谱视图和复习系统中联动使用。
```

- [ ] **Step 2: Commit**

```powershell
git add prompts/README.md
git commit -m "docs: add 考点数据生成专家 prompt reference"
```

---

### Task 9: Manual verification

- [ ] **Step 1: Verify app starts without errors**

```powershell
npm run dev
```

Expected: App starts, console shows seed import logs (if knowledge_points was empty), no crash.

- [ ] **Step 2: Verify knowledge map page**

Navigate to the knowledge map page. Verify:
- The tree shows "考研数学考点汇总" as the textbook
- 高等数学 has 5 chapters, each with sub-points
- 线性代数 has 3 chapters with sub-chapters and points
- Total ~42 knowledge points visible

- [ ] **Step 3: Verify category dropdowns**

Navigate to 错题库 page, open the category filter. Verify the dropdown shows the new categories:
- 函数、极限、连续
- 一元函数微分学
- 一元函数积分学
- 多元函数微积分学
- 常微分方程
- 无穷级数
- 行列式与矩阵
- 线性方程组与向量
- 特征值与二次型
- 概率论
- 其他

- [ ] **Step 4: Verify question form**

Open the new question form. Verify the "章节分类" dropdown shows the same new categories.

- [ ] **Step 5: Verify data migration**

If there are existing questions with old category values (e.g., "重积分", "曲线曲面积分", "微分方程"), verify they have been migrated to the new values after app restart. Check via the 错题库 filter or directly in the database.

- [ ] **Step 6: Verify AI import prompt**

If you have a DeepSeek API key configured, try the AI import flow with a test image. Verify the structured output uses new category values and knowledge_points from the exam point list.
