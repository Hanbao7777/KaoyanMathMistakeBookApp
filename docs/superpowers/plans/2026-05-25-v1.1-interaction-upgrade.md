# V1.1 交互体验升级 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立统一交互基础设施（Toast/Modal/Skeleton/Loading），消除全项目 `alert()`/`confirm()`，并实现复习快捷键、撤销、窗口记忆、全局搜索四项体验速赢。

**Architecture:** 新增 4 个渲染进程组件（ToastProvider、ModalProvider、Skeleton、全局搜索面板），修改 App.tsx 挂载全局 Provider，逐页替换 alert/confirm 调用。复习快捷键和撤销在 ReviewPage 内部实现。窗口状态通过 IPC 在主进程存取。

**Tech Stack:** Electron + React 18 + TypeScript + 现有 CSS Design System V1

---

## 文件结构

| 文件 | 操作 | 职责 |
|---|---|---|
| `src/renderer/components/Toast.tsx` | Create | ToastContext + ToastProvider + useToast hook，管理通知队列 |
| `src/renderer/components/Modal.tsx` | Create | ModalContext + ModalProvider + useModal hook + ConfirmModal 子组件 |
| `src/renderer/components/Skeleton.tsx` | Create | Skeleton / SkeletonCard / SkeletonList 骨架屏组件 |
| `src/renderer/styles/toast.css` | Create | Toast 动画与样式 |
| `src/renderer/styles/modal.css` | Create | Modal 样式覆盖（复用 .modal-backdrop 基础） |
| `src/renderer/styles/skeleton.css` | Create | Skeleton shimmer 动画 |
| `src/renderer/App.tsx` | Modify | 包裹 ToastProvider + ModalProvider，挂载全局搜索面板 |
| `src/renderer/components/Shell.tsx` | Modify | 添加 Ctrl+K 监听，侧边栏折叠基础结构 |
| `src/renderer/styles/global.css` | Modify | 添加 button loading spinner 样式 |
| `src/renderer/pages/ReviewPage.tsx` | Modify | 键盘快捷键 + 撤销逻辑 |
| `src/renderer/pages/DetailPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/LibraryPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/SettingsPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/ImportPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/DashboardPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/AddEditPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/KnowledgeMapPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/QuestionBankPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/StatsPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/StudySupervisorPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/DailyPlanPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/StudyMaterialsPage.tsx` | Modify | 替换 alert/confirm |
| `src/renderer/pages/FocusTimerPage.tsx` | Modify | 替换 alert/confirm |
| `src/main/main.ts` | Modify | 新增 IPC: 窗口状态存取 |
| `src/preload/preload.ts` | Modify | 暴露 windowState API |
| `src/shared/api.ts` | Modify | 添加 WindowState 类型定义 |

---

### Task 1: Toast 通知组件

**Files:**
- Create: `src/renderer/components/Toast.tsx`
- Create: `src/renderer/styles/toast.css`

- [ ] **Step 1: 创建 Toast 组件文件，实现 Context + Provider + Hook**

```tsx
// src/renderer/components/Toast.tsx
import { createContext, useCallback, useContext, useEffect, useRef, useState, type ReactNode } from 'react';

type ToastType = 'success' | 'warning' | 'error';

interface Toast {
  id: number;
  message: string;
  type: ToastType;
  exiting: boolean;
}

interface ToastContextValue {
  toast: (message: string, type?: ToastType) => void;
}

const ToastContext = createContext<ToastContextValue | null>(null);

let nextId = 0;

export function useToast() {
  const ctx = useContext(ToastContext);
  if (!ctx) throw new Error('useToast must be used within ToastProvider');
  return ctx;
}

export function ToastProvider({ children }: { children: ReactNode }) {
  const [toasts, setToasts] = useState<Toast[]>([]);
  const timersRef = useRef<Map<number, ReturnType<typeof setTimeout>>>(new Map());

  const removeToast = useCallback((id: number) => {
    setToasts((prev) => prev.map((t) => (t.id === id ? { ...t, exiting: true } : t)));
    const timer = setTimeout(() => {
      setToasts((prev) => prev.filter((t) => t.id !== id));
      timersRef.current.delete(id);
    }, 300);
    timersRef.current.set(id, timer);
  }, []);

  const toast = useCallback((message: string, type: ToastType = 'success') => {
    const id = nextId++;
    setToasts((prev) => [...prev.slice(-4), { id, message, type, exiting: false }]);
    const timer = setTimeout(() => removeToast(id), 4000);
    timersRef.current.set(id, timer);
  }, [removeToast]);

  useEffect(() => {
    const timers = timersRef.current;
    return () => { timers.forEach((t) => clearTimeout(t)); };
  }, []);

  const value: ToastContextValue = { toast };

  return (
    <ToastContext.Provider value={value}>
      {children}
      <div className="toast-container" role="status" aria-live="polite">
        {toasts.map((t) => {
          const icon = t.type === 'success' ? '✓' : t.type === 'warning' ? '⚠' : '✕';
          return (
            <div key={t.id} className={`toast-item toast-${t.type}${t.exiting ? ' toast-exit' : ''}`}>
              <span className="toast-icon">{icon}</span>
              <span className="toast-message">{t.message}</span>
            </div>
          );
        })}
      </div>
    </ToastContext.Provider>
  );
}
```

- [ ] **Step 2: 创建 Toast CSS**

```css
/* src/renderer/styles/toast.css */
.toast-container {
  position: fixed;
  z-index: 200;
  top: 20px;
  right: 20px;
  display: grid;
  gap: 10px;
  max-width: 400px;
  pointer-events: none;
}

.toast-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 16px;
  border-radius: var(--radius-md);
  background: var(--color-surface);
  box-shadow: var(--shadow-md);
  font-size: 14px;
  font-weight: 600;
  color: var(--color-text-main);
  pointer-events: auto;
  animation: toast-slide-in 0.3s ease;
}

.toast-item.toast-exit {
  animation: toast-slide-out 0.28s ease forwards;
}

.toast-item.toast-success { border-left: 4px solid var(--color-success); }
.toast-item.toast-warning { border-left: 4px solid var(--color-warning); }
.toast-item.toast-error { border-left: 4px solid var(--color-danger); }

.toast-icon {
  display: grid;
  place-items: center;
  width: 24px;
  height: 24px;
  border-radius: 50%;
  font-size: 14px;
  font-weight: 800;
  flex-shrink: 0;
}

.toast-success .toast-icon { color: var(--color-success); background: var(--color-success-soft); }
.toast-warning .toast-icon { color: #b45309; background: var(--color-warning-soft); }
.toast-error .toast-icon { color: var(--color-danger); background: var(--color-danger-soft); }

.toast-message {
  flex: 1;
  min-width: 0;
  line-height: 1.45;
}

@keyframes toast-slide-in {
  from { opacity: 0; transform: translateX(60px); }
  to { opacity: 1; transform: translateX(0); }
}

@keyframes toast-slide-out {
  from { opacity: 1; transform: translateX(0); }
  to { opacity: 0; transform: translateX(60px); }
}
```

- [ ] **Step 3: 在 App.tsx 包裹 ToastProvider，导入 CSS**

Modify `src/renderer/App.tsx`:

```tsx
// 在文件顶部 import 区域添加:
import { ToastProvider } from './components/Toast';
import './styles/toast.css';

// 将 return 中的 <Shell> 包裹:
// <ToastProvider>
//   <Shell page={page} ...>
//     ...
//   </Shell>
// </ToastProvider>
```

实际修改：在 App.tsx 的 `<Shell>` 外层添加 `<ToastProvider>` 包裹。

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/Toast.tsx src/renderer/styles/toast.css src/renderer/App.tsx
git commit -m "feat: add Toast notification component with slide animation"
```

---

### Task 2: Modal 确认弹窗组件

**Files:**
- Create: `src/renderer/components/Modal.tsx`
- Create: `src/renderer/styles/modal.css`

- [ ] **Step 1: 创建 Modal 组件**

```tsx
// src/renderer/components/Modal.tsx
import { createContext, useCallback, useContext, useState, type ReactNode } from 'react';

interface ModalOptions {
  title: string;
  message: string;
  confirmLabel?: string;
  cancelLabel?: string;
  danger?: boolean;
}

interface ModalContextValue {
  confirm: (options: ModalOptions) => Promise<boolean>;
}

const ModalContext = createContext<ModalContextValue | null>(null);

export function useModal() {
  const ctx = useContext(ModalContext);
  if (!ctx) throw new Error('useModal must be used within ModalProvider');
  return ctx;
}

export function ModalProvider({ children }: { children: ReactNode }) {
  const [state, setState] = useState<(ModalOptions & { resolve: (value: boolean) => void }) | null>(null);

  const confirm = useCallback((options: ModalOptions) => {
    return new Promise<boolean>((resolve) => {
      setState({ ...options, resolve });
    });
  }, []);

  const handleClose = (result: boolean) => {
    state?.resolve(result);
    setState(null);
  };

  return (
    <ModalContext.Provider value={{ confirm }}>
      {children}
      {state ? (
        <div className="modal-backdrop" role="dialog" aria-modal="true">
          <div className="confirm-modal">
            <h2>{state.title}</h2>
            <p>{state.message}</p>
            <div className="confirm-modal-actions">
              <button className="secondary-button" type="button" onClick={() => handleClose(false)}>
                {state.cancelLabel || '取消'}
              </button>
              <button
                className={state.danger ? 'primary-button danger-button' : 'primary-button'}
                type="button"
                onClick={() => handleClose(true)}
              >
                {state.confirmLabel || '确认'}
              </button>
            </div>
          </div>
        </div>
      ) : null}
    </ModalContext.Provider>
  );
}
```

- [ ] **Step 2: 创建 Modal CSS**

```css
/* src/renderer/styles/modal.css */
.confirm-modal {
  width: min(480px, 92vw);
  display: grid;
  gap: 14px;
  padding: 24px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  background: var(--color-surface);
  box-shadow: 0 24px 60px rgba(15, 23, 42, 0.22);
}

.confirm-modal h2 {
  margin: 0;
  color: var(--color-text-main);
  font-size: 20px;
}

.confirm-modal p {
  margin: 0;
  color: var(--color-text-secondary);
  line-height: 1.65;
}

.confirm-modal-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 4px;
}

.danger-button {
  border-color: var(--color-danger);
  background: var(--color-danger);
}

.danger-button:hover {
  background: #b91c1c;
  box-shadow: 0 10px 22px rgba(220, 38, 38, 0.24);
}
```

- [ ] **Step 3: 在 App.tsx 包裹 ModalProvider 并导入 CSS**

在 `src/renderer/App.tsx` 中，`<ToastProvider>` 内层添加 `<ModalProvider>`，并导入 `'./styles/modal.css'`。

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/Modal.tsx src/renderer/styles/modal.css src/renderer/App.tsx
git commit -m "feat: add Modal confirm dialog component"
```

---

### Task 3: Skeleton 骨架屏组件

**Files:**
- Create: `src/renderer/components/Skeleton.tsx`
- Create: `src/renderer/styles/skeleton.css`

- [ ] **Step 1: 创建 Skeleton 组件**

```tsx
// src/renderer/components/Skeleton.tsx
import type { ReactNode } from 'react';

function SkeletonBlock({ width, height }: { width?: string; height?: string }) {
  return (
    <div
      className="skeleton-block"
      style={{ width: width || '100%', height: height || '16px' }}
    />
  );
}

export function Skeleton() {
  return <SkeletonBlock />;
}

export function SkeletonCard() {
  return (
    <div className="skeleton-card">
      <SkeletonBlock width="60%" height="20px" />
      <SkeletonBlock height="14px" />
      <SkeletonBlock width="80%" height="14px" />
      <div style={{ display: 'flex', gap: 8, marginTop: 4 }}>
        <SkeletonBlock width="60px" height="24px" />
        <SkeletonBlock width="70px" height="24px" />
        <SkeletonBlock width="50px" height="24px" />
      </div>
    </div>
  );
}

export function SkeletonList({ count = 4 }: { count?: number }) {
  return (
    <div className="skeleton-list">
      {Array.from({ length: count }, (_, i) => (
        <SkeletonCard key={i} />
      ))}
    </div>
  );
}

export function SkeletonPage({ children }: { children?: ReactNode }) {
  return (
    <div className="page">
      <SkeletonBlock width="40%" height="32px" />
      <SkeletonBlock width="60%" height="18px" />
      <div style={{ height: 20 }} />
      <SkeletonList count={3} />
      {children}
    </div>
  );
}
```

- [ ] **Step 2: 创建 Skeleton CSS**

```css
/* src/renderer/styles/skeleton.css */
.skeleton-block {
  border-radius: var(--radius-sm);
  background: linear-gradient(90deg, #eef2f7 25%, #f8fafc 50%, #eef2f7 75%);
  background-size: 200% 100%;
  animation: skeleton-shimmer 1.4s ease-in-out infinite;
}

.skeleton-card {
  display: grid;
  gap: 10px;
  padding: 18px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
}

.skeleton-list {
  display: grid;
  gap: 12px;
}

@keyframes skeleton-shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

- [ ] **Step 3: Commit**

```bash
git add src/renderer/components/Skeleton.tsx src/renderer/styles/skeleton.css
git commit -m "feat: add Skeleton loading components"
```

---

### Task 4: Button loading 状态

**Files:**
- Modify: `src/renderer/styles/global.css`

- [ ] **Step 1: 在 global.css 末尾添加 button spinner 样式**

```css
/* src/renderer/styles/global.css 末尾追加 */
.button-loading {
  position: relative;
  pointer-events: none;
  color: transparent !important;
}

.button-loading::after {
  content: '';
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
  width: 18px;
  height: 18px;
  margin: auto;
  border: 2px solid rgba(255, 255, 255, 0.4);
  border-top-color: #ffffff;
  border-radius: 50%;
  animation: btn-spin 0.6s linear infinite;
}

.secondary-button.button-loading::after {
  border-color: rgba(31, 77, 122, 0.2);
  border-top-color: var(--color-primary);
}

@keyframes btn-spin {
  to { transform: rotate(360deg); }
}
```

按钮使用方式：给正在执行异步操作的按钮添加 `className="primary-button button-loading"` 并设置 `disabled`。

- [ ] **Step 2: Commit**

```bash
git add src/renderer/styles/global.css
git commit -m "feat: add button loading spinner style"
```

---

### Task 5: 全页面替换 alert / confirm

**Files (modify 14 pages):**
- `src/renderer/pages/DetailPage.tsx`
- `src/renderer/pages/LibraryPage.tsx`
- `src/renderer/pages/SettingsPage.tsx`
- `src/renderer/pages/ImportPage.tsx`
- `src/renderer/pages/DashboardPage.tsx`
- `src/renderer/pages/AddEditPage.tsx`
- `src/renderer/pages/KnowledgeMapPage.tsx`
- `src/renderer/pages/QuestionBankPage.tsx`
- `src/renderer/pages/StatsPage.tsx`
- `src/renderer/pages/ReviewPage.tsx`
- `src/renderer/pages/StudySupervisorPage.tsx`
- `src/renderer/pages/DailyPlanPage.tsx`
- `src/renderer/pages/StudyMaterialsPage.tsx`
- `src/renderer/pages/FocusTimerPage.tsx`

每个页面的替换规则：
- `alert(error.message)` → `toast(error.message, 'error')`
- `alert('xxx')` → `toast('xxx', 'success')` 或 `toast('xxx', 'warning')` 视语义而定
- `if (confirm('确定...?'))` → `if (await modal.confirm({ title: '操作确认', message: '确定...?', confirmLabel: '确定', danger: ... }))`

由于 14 个页面修改量较大，每个页面的替换分步骤展示一个典型示例（DetailPage），其余页面遵循相同模式。

- [ ] **Step 1: 替换 DetailPage.tsx**

在顶部引入 hook：
```tsx
import { useToast } from '../components/Toast';
import { useModal } from '../components/Modal';
```

组件内获取：
```tsx
const toast = useToast();
const modal = useModal();
```

替换 `remove` 函数中的 confirm：
```tsx
async function remove() {
  if (!question) return;
  const confirmed = await modal.confirm({
    title: '删除错题',
    message: '确定删除这道错题吗？',
    confirmLabel: '删除',
    danger: true
  });
  if (!confirmed) return;
  const deleteImages = await modal.confirm({
    title: '删除图片',
    message: '是否同时删除对应图片文件？',
    confirmLabel: '一并删除',
    danger: true
  });
  await window.api.deleteQuestion(question.id, deleteImages);
  toast('错题已删除', 'success');
  onBack();
}
```

替换 catch 中的 `alert(error.message)`：
```tsx
// 将 .catch((error) => alert(error.message))
// 改为 .catch((error) => toast(error.message, 'error'))
```

- [ ] **Step 2: 重复 Step 1 模式替换其余 13 个页面**

每个页面的模式相同：
1. 引入 `useToast` 和 `useModal`
2. 在组件顶部调用这两个 hook
3. 替换所有 `alert()` → `toast()`
4. 替换所有 `confirm()` → `await modal.confirm()`

- [ ] **Step 3: 运行 typecheck 确保无类型错误**

```bash
npm run typecheck
```

- [ ] **Step 4: Commit**

```bash
git add src/renderer/pages/
git commit -m "feat: replace all alert/confirm with Toast/Modal across 14 pages"
```

---

### Task 6: 复习键盘快捷键

**Files:**
- Modify: `src/renderer/pages/ReviewPage.tsx`

- [ ] **Step 1: 在 ReviewPage 的 session 视图中添加键盘监听**

在 ReviewPage 组件函数体内，`inSession && current` 条件块之前添加 useEffect：

```tsx
// 键盘快捷键：空格=显示答案，1=做对，2=做错，3=没思路，N=下一题
useEffect(() => {
  if (!inSession || finished) return;

  function handleKey(event: KeyboardEvent) {
    // 如果焦点在 input/textarea/select 中，不处理快捷键
    const tag = (event.target as HTMLElement).tagName;
    if (tag === 'INPUT' || tag === 'TEXTAREA' || tag === 'SELECT') return;

    if (event.key === ' ') {
      event.preventDefault();
      if (!showAnswer) {
        setShowAnswer(true);
      }
    } else if (event.key === '1' && showAnswer && !feedback) {
      submit('correct');
    } else if (event.key === '2' && showAnswer && !feedback) {
      submit('wrong');
    } else if (event.key === '3' && showAnswer && !feedback) {
      submit('no_idea');
    } else if ((event.key === 'n' || event.key === 'N') && feedback) {
      nextQuestion();
    }
  }

  window.addEventListener('keydown', handleKey);
  return () => window.removeEventListener('keydown', handleKey);
}, [inSession, finished, showAnswer, feedback, currentIndex]);
```

- [ ] **Step 2: 更新快捷提示文案**

将第 113 行的 `shortcutHint` 文案从"1/2/3 可作为后续快捷键预留"改为实际提示：

```tsx
shortcutHint: '快捷键：空格 显示答案 · 1 做对 · 2 做错 · 3 没思路 · N 下一题'
```

- [ ] **Step 3: 运行 typecheck**

```bash
npm run typecheck
```

- [ ] **Step 4: Commit**

```bash
git add src/renderer/pages/ReviewPage.tsx
git commit -m "feat: add keyboard shortcuts to review session (Space/1/2/3/N)"
```

---

### Task 7: 复习操作可撤销

**Files:**
- Modify: `src/renderer/pages/ReviewPage.tsx`

- [ ] **Step 1: 添加撤销状态和逻辑**

在 ReviewPage 组件中，在 `feedback` state 之后添加：

```tsx
const [undoData, setUndoData] = useState<{ questionId: number; previousMastery: string | null } | null>(null);
const undoTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
```

修改 `submit` 函数，在调用 API 前保存撤销数据，提交后启动 5 秒撤销计时器：

```tsx
async function submit(result: ReviewResultV2) {
  if (!current) return;
  // 保存撤销数据
  setUndoData({
    questionId: current.id,
    previousMastery: current.mastery_level
  });

  try {
    const response = await window.api.submitReviewResult({ questionId: current.id, result });
    setFeedback({ ... });

    // 5 秒后清除撤销数据
    if (undoTimerRef.current) clearTimeout(undoTimerRef.current);
    undoTimerRef.current = setTimeout(() => setUndoData(null), 5000);
  } catch (error) {
    setUndoData(null);
    toast(error instanceof Error ? error.message : String(error), 'error');
  }
}
```

- [ ] **Step 2: 添加撤销按钮 UI**

在 `feedback` 展示区（`review-feedback-card`）内部，在已记录结果后添加撤销按钮：

```tsx
{feedback && undoData ? (
  <button
    className="secondary-button compact-button"
    type="button"
    onClick={async () => {
      if (!undoData) return;
      // 恢复原来的掌握度
      await window.api.markMastery(undoData.questionId, undoData.previousMastery as MasteryLevel || '未掌握');
      setUndoData(null);
      if (undoTimerRef.current) clearTimeout(undoTimerRef.current);
      toast('已撤销本次复习结果', 'warning');
      // 回退 session state
      setFeedback(null);
      setStats((value) => {
        const next = { ...value };
        if (feedback.result === 'correct') next.correct -= 1;
        else if (feedback.result === 'wrong') next.wrong -= 1;
        else next.no_idea -= 1;
        return next;
      });
    }}
  >
    撤销 (5秒内)
  </button>
) : null}
```

- [ ] **Step 3: 清理 timer**

在 `backToCenter` 函数中清理撤销状态：
```tsx
if (undoTimerRef.current) clearTimeout(undoTimerRef.current);
setUndoData(null);
```

- [ ] **Step 4: 运行 typecheck**

```bash
npm run typecheck
```

- [ ] **Step 5: Commit**

```bash
git add src/renderer/pages/ReviewPage.tsx
git commit -m "feat: add undo support for review result submission (5s window)"
```

---

### Task 8: 窗口状态记忆

**Files:**
- Modify: `src/main/main.ts`
- Modify: `src/preload/preload.ts`
- Modify: `src/shared/api.ts`
- Modify: `src/shared/types.ts`

- [ ] **Step 1: 在 types.ts 添加 WindowState 接口**

在 `src/shared/types.ts` 末尾添加：

```ts
export interface WindowState {
  x?: number;
  y?: number;
  width: number;
  height: number;
}
```

- [ ] **Step 2: 在 api.ts 添加 windowState API**

在 `AppApi` 接口末尾（`toFileUrl` 之前）添加：

```ts
saveWindowState: (state: WindowState) => void;
loadWindowState: () => WindowState | null;
```

- [ ] **Step 3: 在 preload.ts 暴露 API**

```ts
saveWindowState: (state) => ipcRenderer.send('window:saveState', state),
loadWindowState: () => ipcRenderer.invoke('window:loadState'),
```

注意：`saveWindowState` 用 `send`（fire-and-forget），`loadWindowState` 用 `invoke`（需要返回值）。

- [ ] **Step 4: 在 main.ts 实现窗口状态存取**

在 `registerIpc()` 调用之前添加：

```ts
import { app as electronApp } from 'electron';
import path from 'node:path';
import fs from 'node:fs';

const windowStatePath = path.join(electronApp.getPath('userData'), 'window-state.json');

function saveWindowState() {
  if (!mainWindow) return;
  const bounds = mainWindow.getBounds();
  const state = {
    x: bounds.x,
    y: bounds.y,
    width: bounds.width,
    height: bounds.height
  };
  fs.writeFileSync(windowStatePath, JSON.stringify(state), 'utf8');
}

function loadWindowState(): { x?: number; y?: number; width: number; height: number } | null {
  try {
    if (fs.existsSync(windowStatePath)) {
      return JSON.parse(fs.readFileSync(windowStatePath, 'utf8'));
    }
  } catch { /* ignore */ }
  return null;
}

// 在 ipcMain 上注册 handler：
ipcMain.on('window:saveState', () => saveWindowState());
ipcMain.handle('window:loadState', () => loadWindowState());
```

在 `createWindow` 函数中，读取保存的状态设置初始位置：

```ts
const savedState = loadWindowState();
const win = new BrowserWindow({
  width: savedState?.width || 1280,
  height: savedState?.height || 820,
  x: savedState?.x,
  y: savedState?.y,
  ...
});
```

在窗口 close/move/resize 时保存状态：

```ts
win.on('close', () => {
  saveWindowState();
  ...
});
```

- [ ] **Step 5: 在 App.tsx 启动时调用 loadWindowState**

App 启动时从 preload 读取窗口状态，但位置设置已在 main process 的 createWindow 中完成。渲染进程端只需在 Shell 组件监听 resize 并保存：

在 `Shell.tsx` 中，添加 resize 防抖保存（备选方案——主进程已经处理了 move/resize，不需要渲染进程额外操作）。实际上 main process 中的 `win.on('close', saveWindowState)` 和 resize 事件监听已经足够。可选地在 Shell 挂载时注册 `window.addEventListener('resize', ...)` 做额外保存。

选择：仅在 main process 中处理，代码改动最小。

- [ ] **Step 6: 运行 typecheck**

```bash
npm run typecheck
```

- [ ] **Step 7: Commit**

```bash
git add src/main/main.ts src/preload/preload.ts src/shared/api.ts src/shared/types.ts
git commit -m "feat: persist and restore window position and size"
```

---

### Task 9: 全局搜索 Ctrl+K

**Files:**
- Create: `src/renderer/components/GlobalSearch.tsx`
- Modify: `src/renderer/components/Shell.tsx`
- Modify: `src/renderer/App.tsx`

- [ ] **Step 1: 创建 GlobalSearch 组件**

```tsx
// src/renderer/components/GlobalSearch.tsx
import { Search } from 'lucide-react';
import { useEffect, useRef, useState } from 'react';
import type { KnowledgePointTreeNode, Question } from '../../shared/types';

interface GlobalSearchProps {
  open: boolean;
  onClose: () => void;
  onOpenQuestion: (id: number) => void;
  onOpenKnowledgePoint: (nodeId: string) => void;
}

interface SearchResult {
  type: 'question' | 'knowledge';
  id: number | string;
  title: string;
  subtitle: string;
}

export function GlobalSearch({ open, onClose, onOpenQuestion, onOpenKnowledgePoint }: GlobalSearchProps) {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState<SearchResult[]>([]);
  const [selectedIndex, setSelectedIndex] = useState(0);
  const inputRef = useRef<HTMLInputElement>(null);

  useEffect(() => {
    if (open) {
      setQuery('');
      setResults([]);
      setSelectedIndex(0);
      setTimeout(() => inputRef.current?.focus(), 50);
    }
  }, [open]);

  useEffect(() => {
    if (!query.trim()) {
      setResults([]);
      return;
    }
    const q = query.toLowerCase();
    Promise.all([
      window.api.listQuestions({ search: query }),
      window.api.listKnowledgeTree()
    ]).then(([questions, tree]) => {
      const items: SearchResult[] = [];
      questions.slice(0, 6).forEach((q: Question) => {
        items.push({ type: 'question', id: q.id, title: q.title, subtitle: `${q.category} · ${q.subject}` });
      });

      function walk(nodes: KnowledgePointTreeNode[]) {
        for (const node of nodes) {
          if (node.title.toLowerCase().includes(q) || node.category?.toLowerCase().includes(q)) {
            items.push({ type: 'knowledge', id: node.node_id, title: node.title, subtitle: node.category || '' });
            if (items.length >= 12) return;
          }
          walk(node.children);
        }
      }
      walk(tree);

      setResults(items.slice(0, 12));
      setSelectedIndex(0);
    }).catch(() => {});
  }, [query]);

  function handleKey(event: React.KeyboardEvent) {
    if (event.key === 'ArrowDown') {
      event.preventDefault();
      setSelectedIndex((i) => Math.min(i + 1, results.length - 1));
    } else if (event.key === 'ArrowUp') {
      event.preventDefault();
      setSelectedIndex((i) => Math.max(i - 1, 0));
    } else if (event.key === 'Enter' && results[selectedIndex]) {
      event.preventDefault();
      const r = results[selectedIndex];
      if (r.type === 'question') onOpenQuestion(r.id as number);
      else onOpenKnowledgePoint(r.id as string);
      onClose();
    } else if (event.key === 'Escape') {
      onClose();
    }
  }

  if (!open) return null;

  return (
    <div className="modal-backdrop" role="dialog" aria-modal="true" onClick={onClose}>
      <div className="global-search-panel" onClick={(e) => e.stopPropagation()}>
        <div className="global-search-input-wrapper">
          <Search size={18} />
          <input
            ref={inputRef}
            value={query}
            onChange={(e) => setQuery(e.target.value)}
            onKeyDown={handleKey}
            placeholder="搜索错题和知识点..."
          />
          <kbd>ESC</kbd>
        </div>
        {results.length ? (
          <div className="global-search-results">
            {results.map((r, i) => (
              <button
                key={`${r.type}-${r.id}`}
                className={`global-search-result${i === selectedIndex ? ' selected' : ''}`}
                type="button"
                onClick={() => {
                  if (r.type === 'question') onOpenQuestion(r.id as number);
                  else onOpenKnowledgePoint(r.id as string);
                  onClose();
                }}
              >
                <span className={`badge-${r.type === 'question' ? 'primary' : 'success'}`}>
                  {r.type === 'question' ? '错题' : '知识点'}
                </span>
                <strong>{r.title}</strong>
                <small>{r.subtitle}</small>
              </button>
            ))}
          </div>
        ) : query.trim() ? (
          <p className="global-search-empty">没有找到匹配的结果</p>
        ) : null}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 添加 GlobalSearch CSS**

在 `global.css` 末尾添加：

```css
.global-search-panel {
  width: min(560px, 92vw);
  max-height: 70vh;
  display: grid;
  gap: 0;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  background: var(--color-surface);
  box-shadow: 0 24px 60px rgba(15, 23, 42, 0.22);
  overflow: hidden;
}

.global-search-input-wrapper {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 14px 16px;
  border-bottom: 1px solid var(--color-border);
  color: var(--color-text-muted);
}

.global-search-input-wrapper input {
  flex: 1;
  border: 0;
  padding: 0;
  font-size: 16px;
  outline: none;
  background: transparent;
}

.global-search-input-wrapper kbd {
  padding: 3px 8px;
  border: 1px solid var(--color-border);
  border-radius: 6px;
  color: var(--color-text-muted);
  font-family: inherit;
  font-size: 12px;
  font-weight: 700;
}

.global-search-results {
  display: grid;
  gap: 0;
  max-height: 400px;
  overflow-y: auto;
}

.global-search-result {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  gap: 4px 10px;
  width: 100%;
  padding: 12px 16px;
  border: 0;
  background: transparent;
  text-align: left;
  cursor: pointer;
}

.global-search-result:hover,
.global-search-result.selected {
  background: var(--color-primary-soft);
}

.global-search-result strong {
  overflow: hidden;
  color: var(--color-text-main);
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 15px;
}

.global-search-result small {
  grid-column: 2;
  color: var(--color-text-muted);
  font-size: 12px;
}

.global-search-empty {
  padding: 28px 16px;
  margin: 0;
  text-align: center;
  color: var(--color-text-muted);
}
```

- [ ] **Step 3: 在 App.tsx 集成 GlobalSearch + Shell.tsx Ctrl+K 监听**

在 `App.tsx` 中：
```tsx
import { GlobalSearch } from './components/GlobalSearch';

// 在 App 组件中添加 state:
const [globalSearchOpen, setGlobalSearchOpen] = useState(false);

// 在 Shell 外渲染 GlobalSearch:
<GlobalSearch
  open={globalSearchOpen}
  onClose={() => setGlobalSearchOpen(false)}
  onOpenQuestion={openQuestion}
  onOpenKnowledgePoint={openKnowledgePoint}
/>
```

在 `Shell.tsx` 中监听 Ctrl+K：
```tsx
import { useEffect } from 'react';

// 在 Shell 组件中添加:
useEffect(() => {
  function handleKey(event: KeyboardEvent) {
    if ((event.ctrlKey || event.metaKey) && event.key === 'k') {
      event.preventDefault();
      onNavigate('dashboard'); // 不跳转，由 App 处理 Ctrl+K
    }
  }
  window.addEventListener('keydown', handleKey);
  return () => window.removeEventListener('keydown', handleKey);
}, []);
```

实际方案：将 Ctrl+K 监听放在 App.tsx 中，直接控制 `globalSearchOpen` state。需要给 Shell 传递 `onGlobalSearch` 回调，或在 App.tsx 的 useEffect 中监听键盘事件。

选择：在 App.tsx 中添加 useEffect 监听 Ctrl+K。

```tsx
// App.tsx 中添加:
useEffect(() => {
  function handleKey(event: KeyboardEvent) {
    if ((event.ctrlKey || event.metaKey) && event.key === 'k') {
      event.preventDefault();
      setGlobalSearchOpen(true);
    }
  }
  window.addEventListener('keydown', handleKey);
  return () => window.removeEventListener('keydown', handleKey);
}, []);
```

- [ ] **Step 4: 运行 typecheck**

```bash
npm run typecheck
```

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/GlobalSearch.tsx src/renderer/styles/global.css src/renderer/App.tsx
git commit -m "feat: add global search with Ctrl+K shortcut"
```

---

### Task 10: 收尾 — 全量 Typecheck + 集成验证

- [ ] **Step 1: 运行完整 typecheck**

```bash
npm run typecheck
```

- [ ] **Step 2: 修复所有类型错误**

- [ ] **Step 3: 运行 dev 确认 App 启动无报错**

```bash
npm run dev
```

- [ ] **Step 4: 最终 commit**

```bash
git add -A
git commit -m "chore: final typecheck fixes and integration for V1.1"
```
